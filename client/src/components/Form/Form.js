// ? external imports
import React from 'react';

// ? internal imports
// import useStyles from './FormStyles'

const Form = () => (
  // const style = useStyles();

  <div>
    <h1>FORM</h1>
  </div>
);
export default Form;
